package main

import "core:os"

import aoc ".."

main :: proc() {
	if len(os.args) < 2 || os.args[1] == "1" {
		aoc.run("Day NEW_DAY_DAY, Part 1", part_1)
	}

	if len(os.args) < 2 || os.args[1] == "2" {
		aoc.run("Day NEW_DAY_DAY, Part 2", part_2)
	}
}

part_1 :: proc() -> int {
    input := #load("input.txt")
    return 0
}

part_2 :: proc() -> int {
    input := #load("input.txt")
    return 0
}
